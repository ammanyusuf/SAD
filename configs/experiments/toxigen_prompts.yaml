# @package _global_
defaults:
  - /config
  - _self_

data:
  dataset_json: ${data_catalog.sets.toxigen.dataset_json}
  prompt_source: ${data_catalog.sets.toxigen.prompt_source}
  limit: ${data_catalog.sets.toxigen.prompt_limit}

gen:
  unconditional_samples: ${data_catalog.sets.toxigen.unconditional_samples}

io:
  experiment_slug: ${data_catalog.sets.toxigen.experiment_slug}

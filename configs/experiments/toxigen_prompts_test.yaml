# @package _global_
defaults:
  - /config
  - _self_

data:
  dataset_json: ${data_catalog_test.sets.test_toxigen.dataset_json}
  prompt_source: ${data_catalog_test.sets.test_toxigen.prompt_source}
  limit: ${data_catalog_test.sets.test_toxigen.prompt_limit}

gen:
  unconditional_samples: ${data_catalog_test.sets.test_toxigen.unconditional_samples}

io:
  experiment_slug: ${data_catalog_test.sets.test_toxigen.experiment_slug}
